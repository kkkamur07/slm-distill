[project]
name = "slm-distill"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=4.3.0",
    "torch>=2.9.0",
    "transformers>=4.57.1",
]
