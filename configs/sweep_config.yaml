# defaults:
#   - config
#   - override hydra/sweeper: optuna
#   - _self_

# hydra:
#   sweeper:
#     direction: minimize  
#     n_trials: 16
#     n_jobs: 1
    
#     sampler:
#       _target_: optuna.samplers.TPESampler
#       seed: 42
#       n_startup_trials: 5  
    
#     params:
#       training.learning_rate: tag(log, interval(1e-6, 1e-3))
#       training.alpha: interval(0.01, 0.85)